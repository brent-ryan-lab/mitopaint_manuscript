## 2026-08-27
- update mahalanobis distance mitopaint data (mean per well) v1 to remove brackets in md lmme contrast character string to avoid NAs in condition lookup metadata
- add hits- mahalanobis distance mitopaint vis (mean per well) v1 to plot dot plot of all md lmme hits, and to plot md lmme p value dose response grid


## 2026-08-24
- update batch integration evaluation stats mitopaint data (mean per well) v1 to calculate asw, gc, kbet, map, lisi, leiden, save summary results table and plot figure visualising all metrics together
- add batch integration evaluation stats mitopaint data (mean per well) v1

## 2026-08-21
- update dimensionality reduction mitopaint data (mean per well) v1 to save neighbour graphs, and rename file to v2 to reflect change

## 2026-08-20
- update tidy raw mitopaint data (mean per well) v1 to make sure remove NA also removes non finite values (sources of NA errors later in pipeline), and to remove conditions based on rm_cond
- update remove redundant mitopaint data (mean per well) v1 to make sure remove features with low variances also removes features with NA variance
- update  batch integration mitopaint vis (mean per well) v1 to remove NA and avoid errors with dimred
- update remove redundant mitopaint vis (mean per well) v1 to subset data before calculating PCA to just DMSO, CCCP, ROT
- update pca- dim red mitopaint vis (mean per well) v2 to include cols if statement to decide whether to use viridis or base ggplot colors depending on colorvar length, and to use fixed lefend space function to save legend separately if legend is long
- update batch integration mitopaint vis (mean per well) v1 to save legends separately

## 2026-08-19
- update robust zscore norm mitopaint vis (mean per well) v1 to explicitly make x axes labels 45 degree, scientific notation for raw values, align plots and fix aspect ratio
- update plate drift correct mitopaint vis (mean per well) v1 to avoid ties when selecting ids for qc plots, add perc_width and perc_height variables for saving dims stacked bar plot
- add abs zscore feature ranking vis (mean per well) v1 to plot features ranked by biggest emm difference between conditions

## 2026-08-18
- update dr- mahalanobis distance classic readouts vis (mean per well) v1 to have black outline for points in dotplot dr

## 2026-08-12
- add similarity heatmap vis (mean per well) v1 to make similarity heatmap of pearson corr of well/ batch level profiles, and save correlation distance clustering stats

## 2026-08-11
- add pca feature loadings- dim red mitopaint vis (mean per well) v1 to plot stacked bar plot of PC loadings by channel

## 2026-08-10
- update mahalanobis distance mitopaint data (mean per well) v1 to plot pca coloured by chi squared hits, chi squared p value and save lmm p values table
- update mahalanobis distance mitopaint data (mean per well) v1 with function to calculate mahalanobis distance hits by chi squared and linear mixed effect model
- add dr- mahalanobis distance classic readouts vis (mean per well) v1 to plot dose response curves/ dot plots of mahalnobis distance or classic readout to compare readout sensitivty

## 2026-08-07
- update classic readouts mitopaint data (mean per well) v1 to save batch combined, non normalised, non readout filtered data
- add classic readouts pca pearson core heatmap vis (mean per well) v1 to make heatmap visualising correlation of classic readouts to PC direction
- add mahalanobis distance mitopaint data (mean per well) v1 to calculate mahalanobis distance to DMSO and plot pca coloured by mahalanobis distance

## 2026-08-06
- add channel corrected spot count mitopaint data (mean per well) v1 to compare z prime without and with post hoc mt-keima ph4 channel correction
- update plate drift raw mitopaint visualisations (mean per well) v1 to apply consistent legend space to plots to avoid squishing from legend title length

## 2026-08-05
- add umap- dim red mitopaint vis (mean per well) v1
- save plots to pca/umap subfolder to classic readouts pca vis, batch integration mitopaint vis
- add legend units, update NN box padding, save plots to pca subfolder to pca- dim red mitopaint vis (mean per well) v2 
- update last edit date on pca- dim red mitopaint vis (mean per well) v2

## 2026-08-04
- update pca-dim red mitopaint vis (mean per well) to instead of manual plot, to use plot_pca function and fixed legend spacing = therefore match visuals of classic readouts pca vis (v1 -> v2)
- added classic readouts pca vis (mean per well) v1 to plot pca coloured to MMP, ROS, Mito Spots and Mito Morph readouts
- update classic readouts mitopaint data (mean per well) v1 to also save all_all = batch combined dmso normalised data not filtered for conditions
- added batch integration mitopaint vis (mean per well) v1 to include pca plot integrated and unintegrated, and umap plot integrated and unintegrated, and all coloured by batch or compound

## 2026-08-03
- added remove redundant mitopaint visualisations (mean per well) v1 to include redu_curve, redu_pie, pca_cutoff_grid, and save plots

## 2026-08-31
- updated plate drift raw mitopaint visualisations (mean per well) v1 to include tidied plots, plot color, and save plots
- added robust zscore norm mitopaint visualisations (mean per well) v1 to include density plot of DMSO feature distributions before and after z score norm, and save plots

## 2026-08-18
- added plate drift raw mitopaint visualisations (mean per well) v1

## 2026-06-19
- added pca biplot- dim red mitopaint vis (mean per well) v1
- added classic readouts mitopaint data (mean per well) v1


## 2026-06-18
- updated pca- dim red mitopaint vis (mean per well) v1 to be generalisable to any dataset, not just mPaintDR2_N
- updated plate drift correct mitopaint visualisations (mean per well) v1 to be generalisable to any dataset, not just mPaintDR2_N
- updated plate drift correct mitopaint visualisations (mean per well) v1 set variables and README variables to include batches_info that can then be generalisable to any number of N

## 2026-06-16
- updated batch integration mitopaint data (N=3) (mean per well) v1 to save metadata with NN labels for PCA, tSNE, UMAP
- updated PCA: dim red mitopaint vis (N=3) (mean per well) mPaintDR2_N2_N3_N4 v1 to plot PCAs coloured by compound, concentration, compound (shape) and conc, and NN (incl label)

## 2026-06-15
- added batch integration mitopaint data (N=3) (mean per well) v1
- data (integrated and unintegrated) from batch integration script save to /data/processed
- added remove redundant mitopaint data (N=3) (mean per well) v1
- data (red) from remove redundant features script save to /data/processed

## 2026-06-12
- added plate drift correct mitopaint visualisations (N=3) (mean per well) v1 to replace Untitled 3
- added variable details in README for using plate drift visualisation scripts
- figures from plate drift visualisation script save to /outputs/figures
- added robust zscore norm mitopaint data (mean per well) v1 to replace Untitled 4
- data from zscore normalisation script save to /data/processed

## 2026-06-11
- added tidy raw mitopaint data (mean per well) v1 script to replace Untitled 1
- added plate drift correct mitopaint data (mean per well) v1 to replace Untitled 2
- added variable details in README for using tidy and plate drift scripts
- processed data from tidy and plate drift scripts save to /data/processed

- scripts now in scripts folder

## 2026-03-19

- commit template of repository architecture (folders)
- created changelog.txt
- created todo.txt


## 2026-03-18

- created mitopaint_manuscript repository